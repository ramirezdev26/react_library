function AboutPage() {
  return (
    <main style={{ textAlign: 'center', paddingTop: '40px' }}>
      <h2>Acerca de</h2>
      <p style={{ marginTop: '12px', color: 'var(--text-muted)' }}>
        Book Dashboard — SPA de búsqueda de libros con React, TypeScript y React Router.
      </p>
    </main>
  );
}

export default AboutPage;
